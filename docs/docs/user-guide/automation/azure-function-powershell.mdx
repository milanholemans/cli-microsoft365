# Run CLI on PowerShell Azure Function 

Getting CLI for Microsoft 365 to work with Azure Functions can make managing Microsoft 365 easier and automate many tasks.
In this guide, you will learn how to setup CLI for Microsoft 365 on an Azure Function using managed identity.

## Prerequisites

In order to follow this guide smoothly, we assume that certain things are already present in your environment.
The tools listed below are used in this guide. While they are not mandatory to use, they recommended when you are following this guide.

- [Visual Studio code](https://code.visualstudio.com)
  - [Azure Resources extension](https://marketplace.visualstudio.com/items?itemName=ms-azuretools.vscode-azureresourcegroups)
  - [Azure Functions extension](https://marketplace.visualstudio.com/items?itemName=ms-azuretools.vscode-azurefunctions)

## Setting up Azure Function

The first thing we have to do is creating an Azure Function. Our goal for this part is to get Node.js running on an Azure Function.

1. Click on the **create a resource** button in the Azure portal.

![Create a resource](../../images/azure-function-powershell/create-a-resource.png)

2. Create a new Function App in Azure.

![Create a Function App](../../images/azure-function-powershell/create-function-app.png)

3. Fill out the required fields of the form and click on the **review + create** button.
While CLI for Microsoft 365 works on multiple platforms, this guide only explains the setup on a PowerShell Azure Function running on Windows.
Note that the version of PowerShell you are targeting doesn't really matter.

![Function App form](../../images/azure-function-powershell/function-app-form.png)

4. Open the Azure Function app.

5. Navigate to **environment veriables**. Over there, add a new environment variable with name `WEBSITE_NODE_DEFAULT_VERSION` and value `~20` to set the right Node version.

:::note

At the time of writing, the LTS version of Node.js is v20.
We recommend to run CLI for Microsoft 365 on the LTS version of Node.js.
Visit the [website of Node.js](https://nodejs.org) to discover the current LTS version of Node.js and update the `WEBSITE_NODE_DEFAULT_VERSION` environment variable accordingly.

:::

![Setting Node environment variable](../../images/azure-function-powershell/environment-variable-node.png)

:::tip

To prevent automatic update checks in your Azure Function, set the `CLIMICROSOFT365_NOUPDATE` environment variable to `1` in the configuration settings.
This will prevent CLI from showing an update message when a new version is available, and it will speed up your execution time.

:::

6. Reboot your Azure Function in order to use the new Node version.

![Reboot Azure Function](../../images/azure-function-powershell/reboot-function.png)

This is it, you have now created an Azure Function App with the right Node version.

## Setting up local project

1. On your local environment, make sure that you have the same Node.js version installed (LTS version) as configured on the Azure Function.

2. Using the Azure plugin from Visual Studio Code, let's create a new Azure Function project.
Select `PowerShell` as the language and `HTTP trigger` as the template. Let's use authorization level `anonymous`.

![Create new project](../../images/azure-function-powershell/create-new-project.png)

3. Create `package.json` file in the root of the project.

Because we want to use CLI for Microsoft 365 in the Azure Function, we need to install it locally in the Azure Function project and upload it to the Azure Function.
This will prevent the need to install the CLI for Microsoft 365 on the Azure Function itself at every execution.
The easiest way to do this is to create a `package.json` file in the root of the project (or run `npm init`) and add the following content:

```json title="package.json"
{
  "name": "azure-function-cli-microsoft-365",
  "version": "1.0.0"
}
```

4. Install CLI for Microsoft 365 in the project.

Run the following command in the terminal at the root of your project:

```sh
npm i @pnp/cli-microsoft365
```

:::note

Note that we don't specify the `-g` flag to install the CLI globally. We want to install it locally in the project.

:::

5. Update `profile.ps1` file.

The `profile.ps1` file in the context of Azure Functions when using PowerShell is a script that runs every time your function app starts up on a new instance.
This behavior is similar to how PowerShell profiles work on your local machine but tailored for the Azure Functions environment.
The primary purpose of `profile.ps1` is to allow you to run initialization code for your function app.
This could include setting up environment variables, loading custom modules, or executing any startup tasks required before your functions can run.
In the case of CLI for Microsoft 365, we need to make sure that the CLI is available in the context of the Azure Function.

Overwrite the content of the `profile.ps1` file with the following content:

```powershell title="profile.ps1"
# Ensure CLI for Microsoft 365 environment path is available in the Azure Function
$functionPath = "$PSScriptRoot\node_modules\.bin" 
if ($Env:PATH.Contains($functionPath) -eq $false) {
  [System.Environment]::SetEnvironmentVariable('PATH', $Env:PATH + ";$functionPath")
}

# Ensure CLI is setup for executing in Azure Function.
m365 setup --scripting --output none
$status = m365 status | ConvertFrom-Json
if ($status -eq "Logged out") {
  m365 login --authType identity
  m365 spo set --url "https://milanhdev.sharepoint.com"
}
```

:::caution

If you don't specify the `setup --scripting --output none` command, you will encounter an error when running the CLI for Microsoft 365 in the Azure Function.

:::
